#include <stdio.h>
/**
 * main - Find the sum of all the multiples of 3 or 5 below 1000
 *
 * Return: value of sum
 */
int main(void)
{
	int n;
	int sum = 0;
	
	for (n = 3; n < 1024; n++)
	{
		if (n % 3 == 0 || n % 5 == 0)
			sum += i;
	}
	return (sum)
}
