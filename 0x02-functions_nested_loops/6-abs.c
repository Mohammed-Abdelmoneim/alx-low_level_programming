#include "main.h"
#include <stdlib.h>
/**
 * _isabs - prints 1 if c is lowercase else prints 0
 *@int: first operand
 *
 * Return: 1 if c is lowercar 0 if else
 */
int _isabs(int c)
{
	int r = abs(c);

	return (r);
}
