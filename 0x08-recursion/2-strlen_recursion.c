#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - returns the length of a string.
 * @s: first param
 *
 * Return: 0 success
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}

